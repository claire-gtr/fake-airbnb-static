<h1>Flats</h1>

<ul>
  <% @flats.each do |flat| %>
  <li>
    <%= flat['id'] %>
    <%= flat['name'] %>
    <%= flat['price'] %>
    <%= link_to 'see more', flat_path(flat['id'])  %>
  </li>
  <% end %>
</ul>
